import Navigation from '../Navigation/Navigation';
import { AppBar } from './Header.styled';

const Header = () => {
  return (
    <AppBar>
      <Navigation />
    </AppBar>
  );
};

export default Header;
